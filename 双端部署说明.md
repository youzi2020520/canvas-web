# Codex-Canvas 0.38.0 双端部署说明

目标：同一项目同时支持：

1. **Codex 客户端（本地优先）**：继续使用原有 STDIO MCP、本地 projectDir、Codex thread、ImageGen 和本地文件收集。
2. **ChatGPT 网页端**：通过 `https://canvas.websyc.tech/mcp` 使用 Streamable HTTP MCP，并可渲染 Codex-Canvas UI。
3. **可选的 Codex 云端模式**：Codex 也可以连接 `https://canvas.websyc.tech/mcp`，和 ChatGPT 使用同一份云端 Canvas。

---

## 一、本地 Codex：无需改变原来的使用方式

根目录执行：

```bash
npm ci
npm start
```

原配置仍然是：

- `.mcp.json`
- `.codex/config.toml`
- `src/mcp-server.mjs`

因此 `collect_recent_images`、`send_to_chat`、本地 `projectDir` 等能力仍然只走本地模式。

---

## 二、服务器：安装网页端双服务

在服务器项目目录执行：

```bash
npm ci
npm install --prefix web-mcp
cp .env.web.example .env.web
```

编辑 `.env.web`：

```dotenv
OPENAI_API_KEY=你的_OpenAI_API_Key
CODEX_CANVAS_PUBLIC_URL=https://canvas.websyc.tech/
CODEX_CANVAS_WEB_PROJECT_DIR=/var/lib/codex-canvas/default
CODEX_CANVAS_IMAGE_MODEL=gpt-image-2
CODEX_CANVAS_TEXT_MODEL=gpt-5.6-luna
CODEX_CANVAS_RUNTIME=web
CODEX_CANVAS_REMOTE_MODE=1
```

`.env.web` 会由 Web Runtime 自动读取，不需要把 API Key 写进前端。

测试启动：

```bash
npm run start:web
```

内部服务默认：

- Canvas UI：`127.0.0.1:43217`
- MCP：`127.0.0.1:8787/mcp`
- MCP 健康检查：`127.0.0.1:8787/health`

---

## 三、生产环境建议 PM2

```bash
pm2 start deploy/ecosystem.config.cjs
pm2 save
```

查看状态：

```bash
pm2 status
pm2 logs codex-canvas-web
pm2 logs codex-canvas-mcp
```

---

## 四、Nginx

参考：

```text
deploy/nginx-canvas.websyc.tech.conf
```

最终公网结构：

```text
https://canvas.websyc.tech/       → Canvas UI
https://canvas.websyc.tech/mcp    → ChatGPT / Codex Remote MCP
https://canvas.websyc.tech/mcp-health → MCP health
```

应用配置后：

```bash
nginx -t
sudo systemctl reload nginx
```

验证：

```bash
curl https://canvas.websyc.tech/mcp-health
```

应返回类似：

```json
{
  "ok": true,
  "name": "codex-canvas-web-mcp"
}
```

---

## 五、ChatGPT 网页端接入

在 ChatGPT Developer Mode / 自定义 Plugin 中填写远程 MCP：

```text
https://canvas.websyc.tech/mcp
```

扫描成功后应该能看到 Web-safe tools：

- `open_canvas`
- `canvas_status`
- `search_canvas`
- `prompt_history`
- `version_groups`
- `add_image`
- `generate_image`
- `edit_image`
- `generate_text`

其中 `open_canvas` 绑定了 MCP App UI Resource；其他工具保持 data-first，不会每次工具调用都重复挂载 Canvas。

---

## 六、Codex 客户端可选连接云端 Canvas

不要改原 `.codex/config.toml` 时，Codex 仍然使用本地完整版。

如果希望 Codex 与 ChatGPT 共用服务器 Canvas，参考：

```text
.codex/config.remote.toml.example
```

核心配置：

```toml
[mcp_servers.codex-canvas-cloud]
url = "https://canvas.websyc.tech/mcp"
startup_timeout_sec = 20
tool_timeout_sec = 180
default_tools_approval_mode = "writes"
```

这时 Codex 与 ChatGPT 都调用同一个远程 MCP，但远程模式不会提供 `send_to_chat`、`collect_recent_images` 等本机能力。

---

## 七、V1 架构限制

当前 Web Runtime 是**单租户 V1**：

```text
CODEX_CANVAS_WEB_PROJECT_DIR=/var/lib/codex-canvas/default
```

适合你自己使用或开发测试。如果以后要给多个用户公开使用，需要继续增加：

- OAuth / 登录用户识别
- `userId → workspaceId` 映射
- 每个用户独立 Canvas 存储
- API 配额 / rate limit
- 审计日志

`CODEX_CANVAS_MCP_TOKEN` 可以给支持 Bearer Header 的客户端做简单保护，但浏览器 Canvas 本身如果公开到互联网，仍建议由现有登录系统、Cloudflare Access、反向代理策略或 OAuth 保护。

---

## 八、这版做了什么

- 保留原有 Codex STDIO MCP，不改变本地插件主链路。
- 新增 `web-mcp/server.mjs`，提供 Streamable HTTP MCP。
- 新增 MCP App UI Resource，让 ChatGPT 可以打开 Canvas UI。
- 新增 GPT Image 2 生成/编辑 Provider。
- 新增 Responses API 文本 Provider。
- 原 Browser Canvas 的图片生成任务在 Web Runtime 下自动改走 OpenAI API，不再启动服务器本机的 `codex exec`。
- Web Runtime 禁用更新、关服、修改本地项目 registry、绑定本机 Codex thread 等本地管理接口。
- Web Runtime 禁止通过 `/api/images` 读取服务器任意本地路径。
- 新增 Nginx、PM2、远程 Codex 配置和健康检查示例。

